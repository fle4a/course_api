---
services:
  postgres:
    image: postgres:15
    container_name: postres_todo-list
    ports:
      - '6500:5432'
    environment:
      PGUSER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_USER: user
      POSTGRES_DB: postgres
    volumes:
      - ./temp/compose/postgres:/var/lib/postgresql/data